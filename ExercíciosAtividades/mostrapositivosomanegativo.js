function countPositivesSumNegatives(input) {
   /* var input = [positivo, negativo]
    var positivo = 0
    var negativo = 0
   if (input >= 0) {
    ++positivo
    }
    else {
    negativo +=input
    }
    return input*/
    var negativo
   for (var input; input<0;negativo.reduce(add)){
       return negativo
   }
   
    // var negativo = input.reduce(add)
   
   var positivo = 0
   if (input >= 0)
   {
       ++positivo
   }

   return input [positivo, negativo]
}

console.log(countPositivesSumNegatives[1,3,5,7,-2,-2,5,12,-3,12])